# Ядро
openai>=1.40.0          # клиент к OpenAI-совместимому API LM Studio
pyyaml>=6.0             # чтение config.yaml
numpy>=1.24            # косинусная близость в RAG

# RAG-эмбеддинги (офлайн, по умолчанию)
sentence-transformers>=2.6.0

# Приблизительный учёт токенов (не обязателен; есть символьный фолбэк)
tiktoken>=0.7.0

# Экспорт детального отчёта в Excel
openpyxl>=3.1.0

# Интерактивный запуск из Jupyter (живой стриминг, прогресс)
ipywidgets>=8.0.0
pandas>=2.0            # просмотр результатов в ноутбуке

# Загрузка локальной модели эмбеддинга (scripts/download_embedder.py).
# Тянет torch — на GPU-ПК это и нужно для быстрых эмбеддингов.
huggingface_hub>=0.23.0
